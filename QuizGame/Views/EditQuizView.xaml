<UserControl x:Class="QuizGame.Views.EditQuizView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:QuizGame.Views"
             mc:Ignorable="d">
    
    <Grid Background="#ECF0F1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Grid Grid.Row="0" Background="#E67E22" Height="80">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <!-- Back Button -->
            <Button Grid.Column="0"
                    Content="←"
                    Command="{Binding BackToMenuCommand}"
                    Background="Transparent"
                    Foreground="White"
                    FontSize="32"
                    FontWeight="Bold"
                    Padding="0,0,0,8"
                    Margin="20,2,0,0"
                    Cursor="Hand"
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center" />
            
            <TextBlock Grid.Column="1" 
                       Text="Edit" 
                       Margin="20,0,0,0"
                       FontSize="28" 
                       FontWeight="Bold" 
                       Foreground="White"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left" />
        </Grid>
        
        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="50,30,50,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Quiz Selection Panel -->
            <Border Grid.Column="0" 
                    Background="White" 
                    CornerRadius="10" 
                    Padding="20"
                    Margin="0,0,20,0"
                    BorderBrush="#BDC3C7"
                    BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Available Quizzes" 
                               FontSize="18" 
                               FontWeight="Bold"
                               Margin="0,0,0,15"
                               Foreground="#2C3E50"/>
                    
                    <ListBox ItemsSource="{Binding AvailableQuizzes}"
                             SelectedItem="{Binding SelectedQuiz}"
                             Height="300"
                             FontSize="14"
                             BorderBrush="#BDC3C7"
                             BorderThickness="1">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Category}" 
                                           Padding="5"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    
                    <Button Content="Load Quiz" 
                            Command="{Binding LoadQuizCommand}"
                            Style="{StaticResource EditButtonStyle}">
                    </Button>
                </StackPanel>
            </Border>
            
            <!-- Question Editor Panel -->
            <ScrollViewer Grid.Column="1" 
                          VerticalScrollBarVisibility="Auto"
                          Visibility="{Binding IsQuizLoadedVisibility}">
                <StackPanel>
                    
                    <!-- Category -->
                    <Border Background="White" 
                            CornerRadius="10" 
                            Padding="30"
                            Margin="0,0,0,20"
                            BorderBrush="#BDC3C7"
                            BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Category" 
                                       FontSize="18" 
                                       FontWeight="Bold"
                                       Margin="0,0,0,10"
                                       Foreground="#2C3E50"/>
                            <TextBox Text="{Binding QuizCategory, UpdateSourceTrigger=PropertyChanged}" 
                                     FontSize="16"
                                     Height="40"
                                     Padding="10"
                                     BorderBrush="#BDC3C7"
                                     BorderThickness="1"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Questions List -->
                    <Border Background="White" 
                            CornerRadius="10" 
                            Padding="30"
                            Margin="0,0,0,20"
                            BorderBrush="#BDC3C7"
                            BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Questions Overview" 
                                       FontSize="18" 
                                       FontWeight="Bold"
                                       Margin="0,0,0,20"
                                       Foreground="#2C3E50"/>
                            
                            <ListBox ItemsSource="{Binding Questions}"
                                     SelectedItem="{Binding SelectedQuestion}"
                                     Height="300"
                                     FontSize="14"
                                     BorderBrush="#BDC3C7"
                                     BorderThickness="1"
                                     Margin="0,0,0,20">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <TextBlock Text="{Binding QuestionNumber, StringFormat='Question {0}'}" 
                                                       FontWeight="Bold"
                                                       Margin="0,0,0,5"/>
                                            <TextBlock Text="{Binding Statement}" 
                                                       TextWrapping="Wrap"
                                                       Foreground="#6C757D"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                    </Border>
                    
                    <!-- Question Editor (shown when question selected) -->
                    <Border Background="White" 
                            CornerRadius="10" 
                            Padding="30"
                            Margin="0,0,0,20"
                            BorderBrush="#BDC3C7"
                            BorderThickness="1"
                            Visibility="{Binding IsQuestionSelectedVisibility}">
                        <StackPanel>
                            <TextBlock Text="{Binding SelectedQuestion.QuestionNumber, StringFormat='Editing Question {0}'}" 
                                       FontSize="18" 
                                       FontWeight="Bold"
                                       Margin="0,0,0,20"
                                       Foreground="#2C3E50"/>
                            
                            <TextBlock Text="Statement:" 
                                       FontSize="14" 
                                       Margin="0,0,0,5"
                                       Foreground="#6C757D"/>
                            <TextBox Text="{Binding SelectedQuestion.Statement, UpdateSourceTrigger=PropertyChanged}" 
                                     FontSize="14"
                                     Height="60"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     Padding="8"
                                     Margin="0,0,0,15"
                                     BorderBrush="#CED4DA"
                                     BorderThickness="1"/>
                            
                            <TextBlock Text="First Answer:" 
                                       FontSize="14" 
                                       Margin="0,0,0,5"
                                       Foreground="#6C757D"/>
                            <TextBox Text="{Binding SelectedQuestion.FirstAnswer, UpdateSourceTrigger=PropertyChanged}" 
                                     FontSize="14"
                                     Height="35"
                                     Padding="8"
                                     Margin="0,0,0,10"
                                     BorderBrush="#CED4DA"
                                     BorderThickness="1"/>
                            
                            <TextBlock Text="Second Answer:" 
                                       FontSize="14" 
                                       Margin="0,0,0,5"
                                       Foreground="#6C757D"/>
                            <TextBox Text="{Binding SelectedQuestion.SecondAnswer, UpdateSourceTrigger=PropertyChanged}" 
                                     FontSize="14"
                                     Height="35"
                                     Padding="8"
                                     Margin="0,0,0,10"
                                     BorderBrush="#CED4DA"
                                     BorderThickness="1"/>
                            
                            <TextBlock Text="Third Answer:" 
                                       FontSize="14" 
                                       Margin="0,0,0,5"
                                       Foreground="#6C757D"/>
                            <TextBox Text="{Binding SelectedQuestion.ThirdAnswer, UpdateSourceTrigger=PropertyChanged}" 
                                     FontSize="14"
                                     Height="35"
                                     Padding="8"
                                     Margin="0,0,0,10"
                                     BorderBrush="#CED4DA"
                                     BorderThickness="1"/>
                            
                            <TextBlock Text="Fourth Answer:" 
                                       FontSize="14" 
                                       Margin="0,0,0,5"
                                       Foreground="#6C757D"/>
                            <TextBox Text="{Binding SelectedQuestion.FourthAnswer, UpdateSourceTrigger=PropertyChanged}" 
                                     FontSize="14"
                                     Height="35"
                                     Padding="8"
                                     Margin="0,0,0,10"
                                     BorderBrush="#CED4DA"
                                     BorderThickness="1" />
                            <TextBlock Text="Correct Answer:" 
                                       FontSize="14" 
                                       Margin="0,0,0,5"
                                       Foreground="#6C757D"/>
                            <ComboBox SelectedItem="{Binding SelectedQuestion.CorrectAnswer, UpdateSourceTrigger=PropertyChanged}"
                                      ItemsSource="{Binding SelectedQuestion.AnswerOptions}"
                                      FontSize="14"
                                      Height="35"
                                      Padding="8"
                                      Margin="0,0,0,20"
                                      BorderBrush="#CED4DA"
                                      BorderThickness="1" />
                            
                            <Button Content="Save Changes" 
                                    Command="{Binding SaveChangesCommand}"
                                    Style="{StaticResource SuccessButtonStyle}">
                            </Button>
                            
                            <TextBlock Text="{Binding StatusMessage}"
                                       Foreground="{Binding StatusColor}"
                                       FontSize="12"
                                       FontWeight="Bold"
                                       Margin="0,15,0,0"
                                       TextWrapping="Wrap"
                                       HorizontalAlignment="Center" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
